using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scoreAndCombo : MonoBehaviour {

          string scoreval;
          string comboval;

        public static int uwu1;
        public static int uwu2;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        /*
        if (lastNoteBehaviour.lastNoteDone == true)
        {
            scoreval = GetComponent<playerScore>().playerScoreAsText.text;
            comboval = GetComponent<playerCombo>().currentCombo.ToString();
            Debug.Log(scoreval);
            Debug.Log(comboval);
        }
            */
        
    }
}
